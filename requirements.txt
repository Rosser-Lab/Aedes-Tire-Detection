# requirements.txt - Only Python packages, no system libraries
# IMPORTANT: PyTorch, torchvision, torchaudio, numpy, scipy come from Sherlock modules
# DO NOT install these via pip to avoid conflicts

# Segmentation and image processing
segmentation-models-pytorch==0.3.3
albumentations==1.3.1
opencv-python>=4.8.0

# Geospatial and data handling (simplified)
rasterio>=1.3.0
shapely>=2.0.0

# Data handling
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization and utilities
matplotlib>=3.7.0
tqdm>=4.65.0
Pillow>=10.0.0

# Optional: for better performance
numba>=0.58.0
safetensors>=0.3.0
